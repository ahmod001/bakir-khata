import { Link } from '@inertiajs/react';

const ForgotPass = () => {
    return (
        <Link href='/forgot-password' className="text-blue-500 hover:underline ms-auto">Forgot password?</Link>
    );
};

export default ForgotPass;